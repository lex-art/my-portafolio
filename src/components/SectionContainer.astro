---
const { class: className, id, maxWidth } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto container ${maxWidth ?? "lg:max-w-3xl"} md:max-w-2xl min-h-dvh`}
>
  <slot />
</section>
